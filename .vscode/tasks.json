{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "options": {
        "env": {
            "CUSTOMLANG": "es",
            "JOBTYPE": "full",
            "NOCUTPAGES": "1"
        }
    },
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "${workspaceFolder}/generate.sh",
            "group": {
                "kind": "build",
                "isDefault": true,
            }
        }
    ]
}